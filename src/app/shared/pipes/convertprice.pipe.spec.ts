import { ConvertpricePipe } from './convertprice.pipe';

describe('ConvertpricePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertpricePipe();
    expect(pipe).toBeTruthy();
  });
});

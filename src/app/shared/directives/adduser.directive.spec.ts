import { AdduserDirective } from './adduser.directive';

describe('AdduserDirective', () => {
  it('should create an instance', () => {
    const directive = new AdduserDirective();
    expect(directive).toBeTruthy();
  });
});
